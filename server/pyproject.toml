[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Toby Gore", email = "tobyedwardgore+git@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.13.5",
    "rich-stdout>=0.1.0",
    "typing-extensions>=4.16.0",
    "uvicorn>=0.53.0",
    "websockets>=17.1",
]

[project.scripts]
server = "server:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
